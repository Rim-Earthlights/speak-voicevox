export const CONFIG = {
    TOKEN: 'YOUR_API_KEY',
    DB: {
        HOSTNAME: 'HOSTNAME',
        USERNAME: 'orange',
        DATABASE: 'orangebot',
        PASSWORD: 'PASSWORD',
        FLUSH: false
    },
    COMMAND: {
        SPEAK: 'speak',
        SPEAKER_CONFIG: {
            ENABLE: true,
            SPEAKER_CONFIG: 'speaker-config',
            SPEAKER_CONFIG_SHORT: 'spcon'
        },
        DISCONNECT: 'discon',
    }
};

export type CommandConfig = {
    TOKEN: string;
    COMMAND: {
        SPEAK: string;
        SPEAKER_CONFIG: {
            ENABLE: boolean;
            SPEAKER_CONFIG: string;
            SPEAKER_CONFIG_SHORT: string;
        },
        DISCONNECT: string;
    }
}